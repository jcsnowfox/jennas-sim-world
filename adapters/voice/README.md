# Voice adapter

Purpose: Browser/OS TTS, Fish Audio cloud/local boundaries, fallback order, playback state, captions, and credential isolation.

Responsibilities: Browser/OS TTS, Fish Audio cloud/local boundaries, fallback order, playback state, captions, and credential isolation.

Permitted dependencies: shared contracts and local API adapter interfaces.

Forbidden dependencies: renderer-held provider secrets, claims of local Fish without verification.

Implementation phase: Phase 9.

Current status: Documented boundary only; no voice implementation.
