# Local API

Purpose: Authenticated local service APIs for world, brain requests, memory retrieval, media jobs, voice routing, touch validation, sync, backups, and rate limits.

Responsibilities: Authenticated local service APIs for world, brain requests, memory retrieval, media jobs, voice routing, touch validation, sync, backups, and rate limits.

Permitted dependencies: shared contracts, persistence, simulation core, adapters through interfaces.

Forbidden dependencies: renderer-only code, direct UI concerns, hard-coded cloud dependencies.

Implementation phase: Phase 1 onward.

Current status: Documented boundary only; no service implementation.
