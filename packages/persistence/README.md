# Persistence

Purpose: PostgreSQL migrations, repository layer, transaction helpers, projection rebuilds, and backup metadata.

Responsibilities: PostgreSQL migrations, repository layer, transaction helpers, projection rebuilds, and backup metadata.

Permitted dependencies: shared contracts, migration tooling, PostgreSQL driver after Phase 2.

Forbidden dependencies: renderer code, direct Electron dependencies, ad hoc runtime schema creation.

Implementation phase: Phase 2.

Current status: Documented boundary only; no schema implementation.
