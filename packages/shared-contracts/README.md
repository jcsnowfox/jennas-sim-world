# Shared contracts

Purpose: Versioned TypeScript contracts for events, world references, privacy labels, adapter interfaces, and validation schemas.

Responsibilities: Versioned TypeScript contracts for events, world references, privacy labels, adapter interfaces, and validation schemas.

Permitted dependencies: none beyond future validation/type libraries.

Forbidden dependencies: runtime implementations, provider SDKs, database clients.

Implementation phase: Phase 1 onward.

Current status: Documented boundary only; no contract implementation.
