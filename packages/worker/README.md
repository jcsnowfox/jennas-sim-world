# Worker

Purpose: Background world clock, needs decay, scheduled actions, offline reconciliation, media preprocessing orchestration, and sync queue execution.

Responsibilities: Background world clock, needs decay, scheduled actions, offline reconciliation, media preprocessing orchestration, and sync queue execution.

Permitted dependencies: simulation core, shared contracts, local API job contracts.

Forbidden dependencies: renderer code, direct UI access, provider secrets in worker messages.

Implementation phase: Phase 7.

Current status: Documented boundary only; no worker implementation.
