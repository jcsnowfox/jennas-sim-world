# Game client

Purpose: Babylon canvas host and accessible HTML interface for lots, camera, UI, settings, cinema controls, and memory viewer.

Responsibilities: Babylon canvas host and accessible HTML interface for lots, camera, UI, settings, cinema controls, and memory viewer.

Permitted dependencies: shared contracts, renderer assets after Phase 4, local API client contracts.

Forbidden dependencies: canonical memories, companion personality, direct database access, provider secrets.

Implementation phase: Phase 4.

Current status: Documented boundary only; no renderer implementation.
